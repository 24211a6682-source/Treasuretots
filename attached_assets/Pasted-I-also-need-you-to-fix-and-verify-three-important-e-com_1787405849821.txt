I also need you to fix and verify three important e-commerce features in the existing TreasureTots website.

**Do not redesign the website or break any existing functionality. Make the smallest necessary changes.**

### 1. Search bar is not working

The search bar currently does not properly search the products.

Please inspect the existing search implementation and fix it so that:

* Users can search by product name.
* Search should work with partial names.
* Search should be case-insensitive.
* Search should handle extra spaces.
* Search should return relevant matching products.
* If there are no matches, show a proper "No products found" message.
* Search results should be clickable and open the correct product.
* Search should work across all existing product categories.
* Do not create a separate/mock product dataset. Use the actual product data already used by the website.
* Make sure search still works after products are added through the admin dashboard.

For example:

`"hanuman"` should find the Hanuman-related products.

`"color"` should find the Coloring Book.

`"flash"` should find relevant flash-card products.

### 2. Recommended / Similar Products on Product Details

When a user opens a product, the product details page should also display a section such as:

**"You May Also Like"**

or

**"Recommended Products"**

The recommendations should be based on the actual products in the store.

Preferably recommend products using relevant attributes such as:

* Same category
* Similar product type
* Related keywords/tags
* Similar use case
* Related products

For example, if the user opens a **Hanuman Chalisa Book**, the page could recommend other devotional books or related learning/devotional products.

If the user opens an **A-Z Phonics Flash Card**, recommend other flash cards or educational products.

Requirements:

* Show several relevant products.
* Do not recommend the product currently being viewed.
* Do not show duplicate products.
* Recommended products must be clickable.
* Clicking a recommendation should open that product's details page.
* Recommendations should update automatically when new products are added.
* Do not hardcode a fixed list of recommendations if the existing product database can be used.

If the project already has categories/tags/product metadata, reuse that system rather than creating another unnecessary system.

### 3. VERIFY UNIQUE ORDER IDs

This is important.

I need you to **actually inspect the current order-generation and database logic** and confirm whether every order receives a unique Order ID.

Do NOT simply tell me that the IDs "should be unique."

Check:

* Where Order IDs are generated.
* Whether the ID is generated by the frontend or backend.
* Whether the database has a uniqueness constraint/index on the Order ID.
* Whether two simultaneous orders could receive the same ID.
* Whether retrying an order/payment could accidentally create duplicate IDs.
* Whether cancelled/failed orders can cause ID collisions.
* Whether existing orders already contain duplicate IDs.

Run an actual database query/check against the existing orders.

The result must explicitly tell me:

**Total orders:** X
**Unique Order IDs:** Y
**Duplicate Order IDs:** Z

If `Z = 0`, confirm that there are currently no duplicate Order IDs.

If duplicates exist, identify them and fix the underlying Order ID generation/database constraint rather than merely changing the displayed ID.

### Order ID requirements

Every successfully created order must have a unique, persistent Order ID.

The uniqueness must be enforced at the backend/database level, not just by frontend JavaScript.

If the current implementation does not guarantee uniqueness, implement a reliable solution using the project's existing database architecture.

Also ensure that:

* Refreshing the page cannot create another order ID.
* Payment retries cannot accidentally reuse an incorrect order.
* Duplicate requests are handled safely.
* Order IDs remain unchanged after the order is created.
* Admin orders and customer orders use the same reliable ID system.
* Existing valid orders are not modified unnecessarily.

### Final verification

After implementing these changes, test all three areas.

Report:

1. **Search**

   * What was broken?
   * What was fixed?
   * Example searches tested.
   * Whether results open correctly.

2. **Recommendations**

   * How recommendations are determined.
   * How many recommendations are displayed.
   * Whether they come from the real product database.

3. **Order IDs**

   * Total orders checked.
   * Unique Order IDs.
   * Duplicate Order IDs.
   * Database uniqueness constraint/index status.
   * How IDs are generated.

4. **Regression check**

   * Homepage
   * Product details
   * Cart
   * Checkout
   * Orders
   * Admin dashboard
   * Payment flow

Do not claim something is working unless you actually test it.
